{
  "name": "blog-api",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/admin",
    "apps/users",
    "apps/shared"
  ],
  "scripts": {
    "install": "npm install",
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.2.3"
  }
}
